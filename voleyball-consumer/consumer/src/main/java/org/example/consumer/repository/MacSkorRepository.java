package org.example.consumer.repository;

import org.example.consumer.model.MacSkor;
import org.springframework.data.jpa.repository.JpaRepository;

public interface MacSkorRepository extends JpaRepository<MacSkor, Long> {
}